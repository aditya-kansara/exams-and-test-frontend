# ====================================
# Frontend Environment Variables
# ====================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# ====================================
# CRITICAL: Required for Production
# ====================================

# Supabase Configuration (REQUIRED)
# Get these from your Supabase project: https://app.supabase.com/
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API Configuration (REQUIRED)
# Development: http://localhost:8000
# Production: Your deployed backend URL (e.g., https://api.yourdomain.com)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# ====================================
# Optional: Payment Configuration
# ====================================

# Stripe Configuration (Client-side)
# Only include the publishable key on the client
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Server-side Stripe secrets (DO NOT expose to client)
# These should ONLY be used in API routes, never in next.config.js
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ====================================
# Deprecated/Unused (Can be removed)
# ====================================
# NEXT_PUBLIC_WS_BASE=ws://localhost:8000  # WebSocket not currently used
# NEXTAUTH_URL=http://localhost:3000  # Using Supabase Auth instead
# NEXTAUTH_SECRET=your_nextauth_secret_here  # Using Supabase Auth instead

# ====================================
# Production Deployment Notes
# ====================================
#
# For Vercel/Netlify/etc:
# 1. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
# 2. Add NEXT_PUBLIC_API_BASE (your production backend URL)
# 3. Add NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY (if using Stripe)
# 4. NEVER add STRIPE_SECRET_KEY to environment variables exposed to the client
#
# For Supabase:
# 1. Go to Authentication → URL Configuration
# 2. Add your production domain to "Site URL"
# 3. Add callback URLs: https://yourdomain.com/auth/callback
# 4. Enable Google OAuth provider if using
#
# For Google OAuth:
# 1. Add production redirect URIs in Google Cloud Console
# 2. https://yourdomain.com/auth/callback
#
# ====================================
